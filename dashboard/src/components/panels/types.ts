/** Module (view) names switchable from the system bar. */
export type PanelKey =
  | 'orbit'      // Command Orbit (primary)
  | 'lineage'    // evolution / lineage map
  | 'memory'     // short/project/long term memory layers
  | 'lab'        // experiment lab
  | 'revenue'    // revenue engine
  | 'qa'         // quality control
  | 'perf'       // performance